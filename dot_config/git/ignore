# Global gitignore — applies to every repo on this machine.
# Managed by chezmoi; edit the source, not the deployed file.

# Claude Code local settings (always machine-specific)
**/.claude/settings.local.json

# Throw-away scratch area (see ~/scratch in workspace-directory-layout.md)
scratch/
