# macOS
.DS_Store
.Spotlight-V100
.Trashes
.fseventsd
.TemporaryItems

# SSH private keys — never commit these
dotfiles/.ssh/id_*
dotfiles/.ssh/known_hosts

# Helix runtime (hundreds of tree-sitter grammar repos — re-downloadable)
dotfiles/.config/helix/runtime/

# Sensitive configs
dotfiles/.claude.json
dotfiles/.config/gcloud/
dotfiles/.config/anthropic/

# AWS credentials
dotfiles/.aws/credentials
