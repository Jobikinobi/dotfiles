# macOS
.DS_Store
.Spotlight-V100
.Trashes
.fseventsd
.TemporaryItems
Library/

# SSH private keys — never commit these
private_dot_ssh/id_*
private_dot_ssh/known_hosts

# Sensitive configs
dot_npmrc
dot_config/gcloud/
dot_config/anthropic/

# AWS credentials
dot_aws/

# Vendored binaries — a 47MB Linux chezmoi build was committed here twice and
# deployed onto macOS (dotfiles#117). Install tools with brew, not by checking
# executables into the source state.
bin/chezmoi

# Local Docker context export — may contain machine-specific connection data.
docker-context.tar.gz

# Test artifacts
Dockerfile.test
#Worktrees
.worktrees

# Vendored binaries — never commit platform-specific executables
bin/chezmoi
